# change to your domain url

example.com {
    encode gzip

    reverse_proxy localhost:8080    # change to your internal adress & port

    log {
            output file /var/log/caddy/access.log
        }
}
