
# Public domain name (replace with your host name).
example.com {
    encode gzip

# Forward all incoming requests to the Ktor backend.
# Adjust the address and port to match your server.
    reverse_proxy localhost:8080

# Access log for incoming HTTP requests.
    log {
            output file /var/log/caddy/access.log
        }
}
